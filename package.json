{
  "name": "diy-synthesizer",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "sudo amixer set PCM 80% && sudo node src/index.js",
    "shutdown": "sudo shutdown -h now",
    "reboot": "sudo shutdown -r now",
    "kill": "sudo killall node"
  },
  "author": "bberak",
  "license": "ISC",
  "dependencies": {
    "node-sfx": "^0.3.0",
    "pigpio": "^2.0.1"
  }
}
